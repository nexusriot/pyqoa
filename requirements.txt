PyQt6>=6.4.0
openai>=1.0.0
markdown>=3.4.0
Pygments>=2.15.0
# Optional — enables vector retrieval for chat memory.
chromadb>=0.5.0
# Optional — exact token counts for the context meter and budget.
tiktoken>=0.7.0
# Optional — store API keys in the OS keyring instead of settings.json.
keyring>=25.0
